spring:
  datasource:
    url: jdbc:postgresql://5.35.83.16:5432/
    name: ${POSTGRES_DB}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
  jpa:
    show-sql: true

minio:
  access-key: ${MINIO_ACCESS}
  secret-key: ${MINIO_SECRET}
  url: ${MINIO_URL}
